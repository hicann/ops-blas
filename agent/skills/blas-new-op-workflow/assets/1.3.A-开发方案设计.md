# aclblas{Xxx} 开发方案设计

## 1. 基本信息

| 项目 | 内容 |
|------|------|
| 算子名称 | aclblas{Xxx} |
| 算子类别 | {Elementwise / Reduction / Conversion} |
| 目标芯片 | {芯片型号} |
| 目标架构 | {archXX} |
| 支持数据类型 | {FP32 / FP16 / BF16} |
| UB 大小 | 248 KB |

## 2. Tiling 策略

### 2.1 算子分类与设计选择

- 算子类别：{Elementwise / Reduction / Conversion}
- 计算特征：{逐元素独立 / 需要归约 / 数据搬运}

### 2.2 多核切分策略

| 维度 | 切分方式 | 说明 |
|------|---------|------|

### 2.3 UB 切分策略

| Buffer | 大小 | 用途 |
|--------|------|------|

### 2.4 TilingData 结构体定义

```cpp
struct {Xxx}TilingData {
    // Tiling 参数
};
```

### 2.5 分支场景覆盖

| 场景 | 条件 | 处理策略 |
|------|------|---------|

## 3. Kernel 设计

### 3.1 编程框架选择

{SIMD membase / SIMD regbase / SIMT}

### 3.2 数据流设计

```
GM → [DataCopy] → UB → [计算] → UB → [DataCopy] → GM
```

### 3.3 关键代码逻辑

```cpp
// Kernel 核心逻辑
```

### 3.4 API 验证记录

| API | 用途 | 参数约束 | 文档验证 | 结论 |
|-----|------|---------|---------|------|

## 4. Host 代码设计

### 4.1 API 接口

```cpp
aclblasStatus_t aclblas{Xxx}(aclblasHandle handle, {参数列表});
```

### 4.2 参数校验

| 条件 | 返回值 |
|------|--------|

### 4.3 内存管理

| 项目 | 策略 |
|------|------|
| 设备内存 | `aclrtMalloc` + `ACL_MEM_MALLOC_HUGE_FIRST` |
| 数据传输 | `aclrtMemcpy`（H2D / D2H） |
| Tiling 传递 | Host 计算 → `aclrtMemcpy` H2D |

### 4.4 Kernel 调用方式

- `<<<numBlocks, nullptr, stream>>>` 直调
- `aclrtGetDeviceInfo` 动态获取 CoreNum

## 5. 参考算子

| 算子 | 仓库/路径 | 参考要点 |
|------|----------|---------|

## 附录：修订记录

| 版本 | 日期 | 修改内容 |
|------|------|---------|
| v1.0 | {YYYY-MM-DD} | 初始版本 |
